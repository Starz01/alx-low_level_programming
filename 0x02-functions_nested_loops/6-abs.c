#include "main.h"

/**
*_abs - computes the absolute value of an integer
*@m: parameter whose absolute value is computed
*Return: returns positive value
*/

int _abs(int m)
{
	if (m < 0)
	return (-m);
	else
	return (m);






}

